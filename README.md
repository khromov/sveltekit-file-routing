# sveltekit-file-routing

A sample project to demonstrate how a SvelteKit project can run as a standalong html file.

```
nvm use
npm i
npm run build
```

Then double-click on `build/index.html` to open it in a browser.
